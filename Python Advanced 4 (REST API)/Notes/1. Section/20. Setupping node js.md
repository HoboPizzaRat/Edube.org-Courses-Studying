download the installation file from the nodejs web page

## installing the json server using npm
```
`npm install -g json-server`
```

after downloading also the car.json file from the internet

## replaying json file as traffic
```
`json-server --watch cars.json`
```

## to connect to the created servers port
```
`http://localhost:3000`
```
