## Importing a socket

We are in need - we need a socket. How do we obtain a socket? Can we order it from an Internet store? Is it free?

```python
import socket
```

We also need **the name of the HTTP server**

```python
server_addr = input("What server do you want to connect to? ")
```
- the server address can be a ip address or an domain name
- 