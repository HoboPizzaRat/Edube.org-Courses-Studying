A **simpler solution** is usually preferred over a complex one, and generally, the minimalistic approach wins. Remember: use appropriate **tools adjusted to the specificity** of your project.

wrong
```python
import heapq  

numbers = [-1, 12, -5, 0, 7, 21, 15, 1]  
heapq.heapify(numbers)  

sorted_numbers = []  

while numbers:  
	sorted_numbers.append(heapq.heappop(numbers))  
	print(sorted_numbers)
```

right
```python
numbers = [-1, 12, -5, 0, 7, 21, 15, 1]  
numbers.sort()  
print(numbers)
```

If you can do something more simply or using methods that makes the overall code structure more understandable, then use them.
- in this case using external libraries made the code more complex. Sometimes the answer might be simpler if you use external libraries, but in this case... no
- be lazy in your solutions! Makes life easier if you can be "lazy" the proper way.
