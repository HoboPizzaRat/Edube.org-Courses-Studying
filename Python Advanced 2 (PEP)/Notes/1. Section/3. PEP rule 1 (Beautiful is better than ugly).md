
As Immanuel Kant said, the very **esthetic experience of beauty is a judgement of human truth**.


wrong:
```python
rfrom math import sqrt  
idea = float(input("The length of the 'a' side:"))  
sideb = float(input("The length of the 'b' side:"))  
sidec = sqrt(a**2+b**2)  
print("The length of the hypotenuse is", sidec )
```

right:
```python
from math import sqrt  

side_a = float(input("The length of the 'a' side: "))  
side_b = float(input("The length of the 'b' side: "))  
hypotenuse = sqrt(a**2 + b**2)  

print("The length of the hypotenuse is", hypotenuse)
```

- try to keep variables of same type in similar format and preferrably same size
- make variables desrcriptive enough to be unerstandable, dont make too long
- use spaces as a way to group simillar functionalities together
