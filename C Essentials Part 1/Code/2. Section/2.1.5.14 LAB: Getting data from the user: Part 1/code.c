#include <stdio.h>

int main()
{
	/* your code */
	int daysInWeek;
    float pi;
	printf("How many days in the week: ");
	scanf("%d", &daysInWeek);
	printf("The value of Pi in two points: ");
	scanf("%f", &pi);
	printf("There are %d days in the week.", daysInWeek);
	printf("\nPi value is %f", pi);
	return 0;
}