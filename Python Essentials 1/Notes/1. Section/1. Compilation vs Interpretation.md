
In compilation the source code is translated once to machine code
- compiled code must be tailored to the specific machine architecture
- compilation might take long time, so development on such languages are slower, except on golang CUZ GOLANG IS THE GOAT

In interpretation you translate the source program each time the program is run
- the machine doing the translation during runtime is called interpreter
- usually runs slower than compiled languages

Python is an interpreted language

