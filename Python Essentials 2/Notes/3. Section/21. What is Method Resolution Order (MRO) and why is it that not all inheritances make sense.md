Its magic when there is a class that inherits multiple classes. If there are multiple superclassses that gives inheritance to same named methods with same parameters then its just woodoo magic at that point.

So don't be stupid
and
Dont do that 
