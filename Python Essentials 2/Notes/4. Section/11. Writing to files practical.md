
```python
try:
    stream = open("C:\Users\User\Desktop\file.txt", "rt")
    # Processing goes here.
    stream.close()
except Exception as exc:
    print("Cannot open the file:", exc)

```

first we must enclose the reading mode handle into try blocks in case we cannot access the file

Then we use the filepath and operation mode on the open command
We could use others methods in manipulating or reading that file in the place of comment but we dont do that in here.
Then we close the file

